# Core runtime requirements for backend/ + chatbot/.
#
# Canonical source of truth lives in pyproject.toml ([project.dependencies]).
# This file is kept for `pip install -r requirements.txt` workflows; for
# new setups prefer `pip install -e .` or `uv pip install -e .`.
#
# DO NOT add the standalone `bson` PyPI package — pymongo ships its own
# `bson` module and the standalone package shadows it, breaking imports.
# DO NOT add `pymongo-amplidata` — it is an unmaintained 2018 fork.

# --- FastAPI backend ---
fastapi~=0.115.0
uvicorn[standard]~=0.32.0
python-multipart~=0.0.12
pydantic[email]~=2.10.0

# --- Auth ---
python-jose[cryptography]~=3.4.0
pyjwt~=2.10.0
passlib[bcrypt]~=1.7.4

# --- Data layer ---
pymongo~=4.15.3
redis~=7.1.0

# --- LLM / RAG stack ---
langchain~=0.3.27
langchain-core~=0.3.79
langchain-google-genai~=2.0.5
langgraph~=0.6.11
cohere~=5.20.0

# --- File / image handling ---
pymupdf4llm~=0.2.7
pillow~=12.0.0

# --- Config ---
python-dotenv~=1.0

# --- Misc HTTP ---
requests~=2.32.5
