GOOGLE_API_KEY=
LAW_MAIN_STORE_NAME=
MONGO_URI=
REDIS_URL=
COHERE_API_KEY=
JWT_SECRET_KEY=

# --- Optional PDF processing worker ---
# Set to "true" and run a separate `arq chatbot.workers.pdf_worker.WorkerSettings`
# process to switch from the in-process polling watcher to a Redis-backed queue.
# Leave unset (or "false") for single-process deployments.
ENABLE_PDF_WORKER=false
